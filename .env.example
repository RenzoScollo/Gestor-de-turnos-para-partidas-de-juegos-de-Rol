DB_HOST=127.0.0.1
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña
DB_NAME=hola
DB_PORT=3306
# Puerto donde escucha la API (npm run dev)
PORT=3000

# URL del frontend (Vite) — para CORS
FRONTEND_URL=http://localhost:5173

# Autenticacion JWT
JWT_SECRET=cambiar_por_una_clave_larga_y_secreta
JWT_EXPIRES_IN=2h
